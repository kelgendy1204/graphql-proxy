export const typeDefs = `
    type City {
        type: String!
        id: ID!
        latitude: String
        longitude: String
        name: String!
        translations: String
        browsePage: String
    }
`;
